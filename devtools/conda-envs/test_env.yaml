name: test
channels:

  - conda-forge

  - defaults
dependencies:
    # Base depends
  - python>=3.10
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

